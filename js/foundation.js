/* ================================================
   FOUNDATION.JS — Логіка сторінки Фундаменту
   Поки що порожній — додаватиметься з контентом
   ================================================ */

// Тут буде логіка: тести, прогрес, інтерактивні вправи
// по мірі наповнення сторінки контентом

document.addEventListener('DOMContentLoaded', () => {
  console.log('Foundation page loaded');
});
